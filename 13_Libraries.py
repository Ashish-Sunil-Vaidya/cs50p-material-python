# Python Libraries are a set of useful functions that eliminate the need for writing codes from scratch.
# Some modules in default come in library called "random", to use these libraries we import it using key called "import"
# Syntax:  import (LibraryName)

